# AWS re/Start recap quiz python task

The code scans a range of ports, which have to be entered before running the code.As it is a little bit slow the current range is only a few ports from 77 to 81.
There is option to cancel with the keyboard and in case of missing target webpage.
